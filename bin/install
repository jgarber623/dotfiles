#!/usr/bin/env bash

set -e

function puts {
  echo "[ install ] $1"
}

PLAYBOOK_PATH=./playbook.yml
if [[ -n ${1} ]]; then
  PLAYBOOK_PATH="${1}"
fi

puts "Running install script..."

ansible-playbook $PLAYBOOK_PATH

puts "🎉 Done!"

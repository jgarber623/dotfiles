- name: Install packages with Homebrew
  homebrew:
    name: git

- name: Link configuration files
  file:
    src: "{{ role_path }}/files/{{ item }}"
    dest: "~/{{ item }}"
    state: link
    force: yes
  with_items: "{{ git_linked_files }}"
